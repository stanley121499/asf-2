import type { ThemeTokens } from "@/themes/types";

/**
 * Classic boutique tokens — extracted from `constants/theme.ts` (current app palette).
 * Gold accent, white ground, Playfair display pairing remains in `constants/theme` fonts.
 */
export const classicTokens: ThemeTokens = {
  accent: "#C9A96E",
  bg: "#FFFFFF",
  panel: "#F5F5F3",
  text: "#0A0A0A",
  muted: "#6B7280",
  border: "#E5E5E3",
  danger: "#E8453C",
  success: "#22C55E",
  statusBarStyle: "dark",
};
