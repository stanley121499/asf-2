/**
 * The web app used a custom `BottomNavbar` component. In Expo, the bottom bar is
 * implemented with `expo-router` tabs — see `app/(tabs)/_layout.tsx`.
 * This file remains as a structural placeholder per project docs.
 */
export const bottomNavigationImplementedIn = "app/(tabs)/_layout.tsx";
